# Stand vom 30.08.2026 — Übergabe an die nächste Sitzung

Diese Datei fasst zusammen, was in der Sitzung vom 27.–30.08.2026 entstanden
ist. Sie ist zum Lesen am Anfang eines neuen Chatfensters gedacht.

**Nichts steht nur hier.** Aller Code ist committet und auf
`claude/stand-2026-08-22-ji5pkh` gepusht; alle Migrationen sind auf der
Datenbank angewandt. Diese Datei erklärt nur, sie ersetzt nichts — Details
stehen in den jeweils verlinkten Dokumenten.

---

## Wo alles liegt

| | |
|---|---|
| Branch | `claude/stand-2026-08-22-ji5pkh` |
| Letzter Commit | `7ce452e` |
| Supabase-Projekt | `nnfsyuglkqycwenwxmuw` |
| Neue Migrationen dieser Sitzung | `0154`, `0155`, `0156` (angewandt) |
| Lovable-Projekt (B2C, unverändert) | `d5033021-6dce-4044-8bf6-bb50d80aa8ff` |
| Lovable-Projekt (B2B Partner Portal, neu) | `35646ee4-142a-4017-bdb2-089951ddcc94`, Workspace `588f8a86cd6010a58053` |
| Flutter-App | `apps/mobile` |
| Web-Demo | `https://blume1805.github.io/B-rdesnack24-/` — **in dieser Sitzung nicht neu ausgerollt**, keine Änderung an `apps/mobile` |

---

## 1. Design-System-Masterprompt — analysiert, nicht gebaut

Auftrag: vollständiges Next-Generation-Designsystem für Kunden- und
Gesellschafter-App, drei Richtungen zur Wahl, eine Empfehlung, dann an
Lovable weiterleiten.

**Stand:** Analyse fertig, als Artefakt veröffentlicht:
`https://claude.ai/code/artifact/436705d6-23c5-4017-a2d6-4ad5d818fc3e`
(„Drei Richtungen für Bördesnack24"). **Keine der drei Richtungen wurde
vom Auftraggeber ausdrücklich bestätigt.** Der geplante Sendevorgang an
Lovable (Planungsmodus) ist in
`docs/lovable-warteschlange/05-designsystem-neu.md` fertig formuliert
abgelegt, aber **nicht gesendet** — an fehlendem Guthaben gescheitert, wie
später auch der B2B-Auftrag (siehe Abschnitt 3). Teile 2–5 der
Design-System-Umsetzung (Foundations, Components, Motion, Premium) für
beide Apps stehen komplett aus.

**Nächster Schritt, wenn wieder Guthaben da ist:** Erst die Richtung mit
dem Auftraggeber bestätigen (das Artefakt zeigt drei Optionen mit
Vor-/Nachteilen), dann `05-designsystem-neu.md` unverändert senden.

## 2. Advertising-Masterprompt — Backend gebaut, Oberfläche fehlt

Auftrag: regionales Werbe- und Partnernetzwerk in 62 Punkten, mit
Geschäftsmodell, Datenmodell, Preisen. Vollständige Historie mit allen
Entscheidungen, Preisen und Streichungen in
**`docs/ADVERTISING-MASTERPROMPT-ABGLEICH.md`** (Fassung 5) — dort auch die
ausführliche Preisliste (digital/analog/komplett) und das Umsatzmodell für
Werbeflächen.

**Gebaut (Migrationen 0154–0155):** sieben neue Tabellen (`leads`,
`lead_activities`, `advertising_campaigns`, `advertising_creatives`,
`sponsorships`, `advertising_coupon_sponsorships`,
`advertising_redirect_counts`), vier neue Rechte, der Coupon-Anlass als
exklusiver Werbeplatz über einen Datenbank-Ausschluss abgesichert, der
Abschlussbericht mit drei anonymen Kennzahlen (Ausgabe/Aktivierung,
Einlösung, Aufrufe), jeweils ab Mindestzahl 30 ausgewiesen.

**Dabei gefunden und behoben (Migration 0156):** `public.is_admin()` gab
für ein Konto ohne aktives Profil `NULL` statt `false` zurück — dadurch
liess sich jede Prüfung der Form `is_admin() or X` bei falschem `X`
unbemerkt umgehen, in 23 bestehenden Migrationen. Betroffen wäre ein
gesperrtes oder gelöschtes Konto mit noch gültigem Token gewesen. Nachweis:
0 real betroffene Profile. Ausführlich in
`docs/VERARBEITUNGSVERZEICHNIS.md` Abschnitt 4 und als neue Dauerregel in
`CLAUDE.md` (§ „Eine gemeinsame Rollenfunktion kann jede Prüfung stumm
durchlassen").

**Noch nicht gebaut:**
- Die öffentliche Weiterleitungs-Edge-Function für den Klick-Zähler
  (Tabelle und RPC stehen, der Endpunkt `/w/<campaign>` o. ä. fehlt).
- Jede Oberfläche — weder Verwaltung/Flutter (Leads, Kampagnen,
  Sponsoring-Freigabe) noch das Lovable-B2B-Portal (siehe Abschnitt 3,
  dort läuft es gerade an).
- Die Landing Page „Werbung bei Bördesnack24".
- Das Wochenangebot (bewusst zurückgestellt, siehe Antwort 6 im
  Abgleich-Dokument).
- Preisliste (Punkt 1) und Pionierklausel (Punkt 2) sind Vertragsinhalt,
  nicht Code — offen mit dem Auftraggeber zu klären, blockiert die
  Umsetzung aber nicht.

## 3. Lovable B2B Partner Portal — Grundgerüst steht, Guthaben fehlt

**Wichtig:** Dies ist ein **neues, separates** Lovable-Projekt, nicht das
alte B2C-Projekt. Ursprünglich wurde versehentlich angenommen, das alte
Projekt sei das Firmenportal — das war falsch (es ist eine unabhängige
B2C-Konsumenten-App-Kopie) und wurde vor jedem Schaden korrigiert; der
Auftraggeber hat sich für ein neues, separates Projekt entschieden.

Alle Details, der genaue Fortsetzungsauftrag zum Wiedereinfügen und die
Projekt-Zugänge stehen in **`docs/lovable-warteschlange/06-b2b-portal-fachrouten.md`**.
Kurzfassung:

- Zwei Nachrichten sind abgeschlossen: Projekt angelegt, Wissensdokument
  gesetzt (Farben, Typografie, Motion, Geld-Darstellung, erlaubte
  RPC-Liste), Grundgerüst gebaut (Design-System-Tokens, Supabase-Client
  vorbereitet, Demo-Modus, Login, Sidebar-Shell).
- **Der Build ist aktuell rot** — die Fach-Routen (Dashboard, Kontoauszug,
  Firma & Standorte, Verträge, Kampagnen) fehlen noch, der Lovable-Agent
  selbst bat um eine Fortsetzung.
- Der Versuch, diese Fortsetzung zu senden, scheiterte am Guthaben:
  „Lovable can't send this message... out of credits" (Workspace
  `588f8a86cd6010a58053`, https://lovable.dev/settings/billing).
- **Zusätzlich offen, unabhängig vom Guthaben:** Der Supabase-Connector ist
  noch nicht über das Lovable-Dashboard verbunden
  (`https://lovable.dev/dashboard?connectors`) — das kann nur der
  Auftraggeber selbst, kein Agent per MCP.
- **Zu klären beim Fortsetzen:** Ob `docs/lovable-warteschlange/04-b2b-firmenportal.md`
  (ein älterer, eigenständiger Auftrag für Mitarbeiterkonto/Budget am
  Firmenautomaten, nutzt dieselben RPCs `business_dashboard`/
  `business_statement` für einen anderen Anwendungsfall) zusätzlich in
  dieses neue Portal gehört oder getrennt bleibt.

## 4. Infografik und Businessplan — geliefert, Quellen im Repo

Zwei PDF-Dokumente wurden erstellt und im Chat geliefert (nicht als PDF im
Repo, siehe unten): eine A3-quer-Infografik zum Geschäftsmodell und ein
siebenseitiger Businessplan mit Finanzmodell für 2027–2036.

**Der vollständige Rechenweg, alle Annahmen mit Quelle und die
Bauanleitung zum Neu-Erzeugen liegen in `docs/businessplan/README.md`.**
Das ist bewusst so aufgeteilt: Binärdateien (PDFs) nicht ins Git, aber der
Code und die Zahlen, aus denen sie entstanden sind, schon — sonst wäre die
Herleitung nach dieser Sitzung verloren.

**Zwei Korrekturen im zweiten Durchgang** (der Auftraggeber hatte beide
bemängelt, beide waren berechtigt):
- **62 Produkte statt 115.** Die erste Fassung hatte aus einer veralteten
  Preisliste gezählt (`docs/marketing/preisliste_2026-03.csv`). Die
  richtige Zahl kommt direkt aus der Datenbank (`select count(*) from
  public.products` → 62) und deckt sich mit der Migration vom 28.07.2026,
  die den Katalog auf die „verbindliche Preisübersicht" mit 62 Artikeln
  umgestellt hat.
- **Kein Lifetime-Abo im Angebot.** Nur 0,99 €/Monat und 9,99 €/Jahr
  werden zum Start verkauft; das Lifetime-Modell (79,99 € einmalig) steht
  zwar in `app.subscription_plans`, ist aber noch nicht im Vertrieb.

**Gestalterisch im zweiten Durchgang:** Businessplan mit deutlich weniger
Fließtext, fett hervorgehobenen Kennzahlen und Kennzahlen-Reihen statt
Absätzen; Infografik mit dem echten Bördesnack24-Logo (`brand_icon.png`,
das kreisrunde Automat-Icon) zentral im Hero-Bereich, ergänzt um eine aus
`brand_hero.jpg` extrahierte Bördekreis-Umriss-Silhouette als
Hintergrundmotiv.

## 5. Rechtsprüfung dieser Sitzung

Alle Änderungen dieser Sitzung waren entweder Backend-Migrationen mit
eigener Prüfung (siehe `docs/VERARBEITUNGSVERZEICHNIS.md` V18/V19 für die
neuen Werbenetzwerk-Tabellen) oder reine Planungsdokumente/Repo-Doku ohne
neue Datenverarbeitung, neue Kundenfunktion oder neue Abhängigkeit im
Produktcode — dafür jeweils explizit „keine Änderung nötig" mit Begründung
festgehalten. Zwei offene Lücken, beide schon vorher bekannt und nicht neu:
Google als Datenschutz-Empfänger (Support-Postfach), Vereinsbeteiligung
ohne steuerliche Bestätigung.

## 6. Was als Nächstes ansteht (priorisiert)

1. **Lovable-Guthaben auffüllen**, dann Auftrag 06 (B2B-Portal-Fach­routen)
   und ggf. Auftrag 05 (Designsystem) senden.
2. **Supabase-Connector** im neuen Lovable-Projekt verbinden
   (Auftraggeber, Dashboard).
3. **Design-Richtung bestätigen** (Artefakt liegt vor) — blockiert Schritt
   1 für den Designsystem-Teil.
4. **Weiterleitungs-Edge-Function** fürs Werbenetzwerk bauen (Tabelle/RPC
   stehen bereits).
5. **Standortnamen für Automat 5–11** (ab 2030) vom Auftraggeber nachtragen
   lassen — im Businessplan als offen markiert.
6. Alte, unabhängig von dieser Sitzung offene Punkte unverändert: Google
   als Datenschutz-Empfänger, DMARC-Eintrag, `nayax_sales.raw` vor dem
   ersten echten Verkauf prüfen, kein Wiederherstellungstest der
   Sicherungen, sevDesk-Verifikationspflicht, kein anwaltliches Gegenlesen.
